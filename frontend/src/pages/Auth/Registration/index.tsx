import AuthHolder from '..';
import RegisterForm from './Form';

const Registration = () => {
  return (
    <AuthHolder>
      <RegisterForm />
    </AuthHolder>
  );
};

export default Registration;
