import AccountHolder from "../Holder";
import ProfileContainer from "./Container";

const Profile = () => {
  return (
    <AccountHolder>
      <ProfileContainer />
    </AccountHolder>
  );
};

export default Profile;
