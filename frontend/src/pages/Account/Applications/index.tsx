import AccountHolder from '../Holder';
import ApplicationsContainer from './Container';

const Applications = () => {
  return (
    <AccountHolder>
      <ApplicationsContainer />
    </AccountHolder>
  );
};

export default Applications;
