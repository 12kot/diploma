import AccountHolder from '../Holder';
import UsersContainer from './Container';

const Users = () => {
  return (
    <AccountHolder>
      <UsersContainer />
    </AccountHolder>
  );
};

export default Users;
