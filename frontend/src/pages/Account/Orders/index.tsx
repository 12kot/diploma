import AccountHolder from '../Holder';
import OrdersContainer from './Container';

const Orders = () => {
  return (
    <AccountHolder>
      <OrdersContainer />
    </AccountHolder>
  );
};

export default Orders;
