import AccountHolder from '../Holder';
import ForwardersContainer from './Container';

const Forwarders = () => {
  return (
    <AccountHolder>
      <ForwardersContainer />
    </AccountHolder>
  );
};

export default Forwarders;
