import AccountHolder from '../Holder';
import ForwardersContainer from './Container';

const Companies = () => {
  return (
    <AccountHolder>
      <ForwardersContainer />
    </AccountHolder>
  );
};

export default Companies;
