import AccountHolder from '../Holder';
import DriversContainer from './Container';

const Drivers = () => {
  return (
    <AccountHolder>
      <DriversContainer />
    </AccountHolder>
  );
};

export default Drivers;
