import AccountHolder from '../Holder';
import OwnersContainer from './Container';

const Owners = () => {
  return (
    <AccountHolder>
      <OwnersContainer />
    </AccountHolder>
  );
};

export default Owners;
