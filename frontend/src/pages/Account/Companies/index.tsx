import AccountHolder from '../Holder';
import CompaniesContainer from './Container';

const Companies = () => {
  return (
    <AccountHolder>
      <CompaniesContainer />
    </AccountHolder>
  );
};

export default Companies;
